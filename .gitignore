# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*~

# Local env / secrets (never commit Supabase keys)
.env
.env.local

# Internal reference material — kept locally, not published to the public repo/site
*.pdf
*.docx
